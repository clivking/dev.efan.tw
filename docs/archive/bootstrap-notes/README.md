# Bootstrap Notes Archive

This folder stores the earliest rebuild planning notes captured before the
current working-copy runtime was stabilized.

These notes are still useful for historical context, especially for:

- original environment architecture decisions
- early dev/pre/www planning
- cross-machine setup assumptions from the rebuild start

They are no longer the primary operational source of truth.
