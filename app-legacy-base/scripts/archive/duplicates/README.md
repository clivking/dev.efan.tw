# scripts/archive/duplicates

This folder only documents that duplicate script copies existed before.
Corrupted duplicate files have been removed.

Maintain the canonical script only in the root `scripts/` directory.
