# db-old

Place restored legacy database material here before review.

Examples:

- `efan_dev-old.sql`
- table notes
- schema inspection output

Anything in this folder is treated as legacy material and must keep the `-old` suffix until approved.
