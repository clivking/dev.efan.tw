# Reference Metadata

This folder stores scaffold-era metadata files that were bundled with the early
`app/` reference baseline.

They are kept only as reference and are not part of the current active runtime
or working-copy operations.
